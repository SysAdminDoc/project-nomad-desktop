[project]
name = "nomad-field-desk"
version = "7.65.0"
description = "Offline-first preparedness command center"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-x -q --tb=short"

[tool.ruff]
target-version = "py310"
line-length = 120
